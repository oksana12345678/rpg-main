import RegistrationSrc from 'assets/images/registrationImage.png';
import SignetSrc from 'assets/images/signet.png';

import { createImageComponent } from 'utils/createImageComponent';

export const Registration = createImageComponent({ src: RegistrationSrc, alt: 'registration' });
export const Signet = createImageComponent({ src: SignetSrc, alt: 'signet' });
